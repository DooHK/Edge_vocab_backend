# ─────────────────────────────────────────
#  이 파일을 복사해서 application.properties 로 이름 변경 후 값을 채우세요
#  cp application.properties.example application.properties
# ─────────────────────────────────────────

# Datasource (MySQL)
spring.datasource.url=jdbc:mysql://localhost:3306/vocabdb?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
spring.datasource.username=root
spring.datasource.password=YOUR_DB_PASSWORD

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# JWT (64자 이상 Base64 문자열로 교체)
jwt.secret=REPLACE_WITH_BASE64_ENCODED_64CHAR_SECRET
jwt.expiration-ms=2592000000

# Google OAuth Client ID
google.client-id=YOUR_GOOGLE_CLIENT_ID.apps.googleusercontent.com

# CORS (쉼표로 구분)
cors.allowed-origins=https://DooHK.github.io,http://localhost:5500,http://127.0.0.1:5500

# Server
server.port=8080
